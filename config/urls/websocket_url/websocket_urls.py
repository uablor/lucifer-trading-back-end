from django.urls import re_path
# from trading.websocket.consumers import TradeConsumer  # คอนซูมเมอร์สำหรับ WebSocket

websocket_urlpatterns = [
    # pass
    # re_path(r"ws/trade/", TradeConsumer.as_asgi()),  # URL ของ WebSocket
]
