# settings.py

AUTH_USER_MODEL = 'user_config.User'

# import logging

# logger = logging.getLogger(__name__)

# # ปรับระดับ log ให้เป็น DEBUG
# logger.debug(f'AUTH_USER_MODEL is set to: {AUTH_USER_MODEL}')
